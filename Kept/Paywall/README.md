# Paywall — walled module (C9)

Superwall presents; the StoreKit 2 mirror is entitlement truth. **`docs/monetization.md` is
REQUIRED reading before any code lands here** (M8). The core loop never imports this module;
cosmetic streak rewards stay free and outside it. The paywall never gates safety, and reading
your own history is never paywalled (C3).
